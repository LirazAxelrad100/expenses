// Copy this file to config.js and fill in your own Supabase project's values
// (Project Settings → API in the Supabase dashboard). config.js is gitignored
// so your real key never gets committed — on Vercel it's generated at deploy
// time from environment variables instead (see CLAUDE.md).
const SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
const SUPABASE_ANON_KEY = "YOUR-ANON-KEY";
